# Callway local development environment variables (sample)

# Express server
PORT=3001
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Public URL of this server (used for webhooks / callbacks).
# Must be an HTTPS domain reachable from the public Internet (ngrok, tunnel, etc.).
PUBLIC_APP_URL=https://YOUR-NGROK-SUBDOMAIN.ngrok-free.app

# OpenAI Realtime
OPENAI_API_KEY=sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
OPENAI_REALTIME_MODEL=gpt-realtime
REALTIME_MIN_COMMIT_MS=120
REALTIME_MAX_COMMIT_MS=800
REALTIME_USE_SERVER_VAD=0

# Supabase (server-side only)
SUPABASE_URL=https://YOUR-SUPABASE-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...YOUR-SERVICE-ROLE-KEY...

# Twilio Voice + Media Streams
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_PHONE_NUMBER_FROM=+33XXXXXXXXX
# Domain (without protocol) served by ngrok / reverse proxy to reach /api/twilio/media-stream over WSS.
TWILIO_STREAM_DOMAIN=your-ngrok-subdomain.ngrok-free.app
# Optional: enforce that outbound numbers are verified on the Twilio account (useful in trial mode)
TWILIO_REQUIRE_VERIFIED_NUMBERS=0
# Optional: delay (in milliseconds) before triggering the outbound Twilio call
TWILIO_TRIGGER_DELAY_MS=5000

# Optional: force expected OpenAI output format (set to pcm16 to enable bridge conversion fallback)
# OPENAI_OUTPUT_AUDIO_FORMAT=g711_ulaw

# Phone parsing
DEFAULT_PHONE_REGION=FR
PHONE_FALLBACK_REGIONS=FR,BE,CH,DE,AT,LU,NL,IE,GB,ES,PT,IT,DK,SE,NO,FI,US,CA

# Web agent
WEB_END_SESSION_DELAY_MS=5000
